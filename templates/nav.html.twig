<nav class="navbar navbar-expand-lg navbar-light nav-bg">
  <img src="images/zombie.png" alt="zombieLogo" class="img-fluid mb-3" style="max-width: 50px;">
  <h3 class="navbar-brand nav-text">Who Is Your Favorite Walker?</h3>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNav">
    <ul class="navbar-nav">
      <li class="nav-item">
        <a class="nav-link {{ app.request.get('_route') == 'homepage' ? 'active' : '' }}" href="{{ path('homepage') }}">Home</a>
      </li>
      <li class="nav-item">
        <a class="nav-link {{ app.request.get('_route') == 'aboutpage' ? 'active' : '' }}" href="{{ path('aboutpage') }}">About</a>
      </li>
      <li class="nav-item">
        <a class="nav-link {{ app.request.get('_route') == 'zlistpage' ? 'active' : '' }}" href="{{ path('zlistpage') }}">Wall of the Dead</a>
      </li>
      <li class="nav-item">
        <a class="nav-link {{ app.request.get('_route') == 'zformpage' ? 'active' : '' }}" href="{{ path('zformpage') }}">Add your Walker</a>
      </li>
      <li class="nav-item">
        <a class="nav-link {{ app.request.get('_route') == 'admin_panel' ? 'active' : '' }}" href="{{ path('admin_panel') }}">Admin</a>
      </li>
    </ul>

    <!-- User Authentication Status and Logout/Login Buttons -->
    <ul class="navbar-nav ms-auto">
        {% if app.user %}
            <li class="nav-item">
                <span class="nav-link">Logged in as {{ app.user.userIdentifier }}</span>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('logout_confirm') }}">Logout</a>
            </li>
        {% else %}
            <li class="nav-item">
                <a class="nav-link" href="{{ path('login') }}">Login</a>
            </li>
        {% endif %}
    </ul>
  </div>
</nav>